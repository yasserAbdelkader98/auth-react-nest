# Fullstack App – React + NestJS

This repo contains:

- `client/`: Frontend React TypeScript App
- `server/`: Backend NestJS API


# 1. Clone the repo
```bash
git clone ...url
cd auth-react-nest


# Setup
cd client
create .env file and fill it like the .env.example
npm install => install dependences
npm run dev => to run the project

cd server
create .env file and fill it like the .env.example
npm install => install dependences
npm run dev => to run the project